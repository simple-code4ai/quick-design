# 可交互蓝图(完整规格)

> 「quick-design」skill 的规格本体。产出物:可交互蓝图 = 交互式 HTML 设计稿。
> 单文件自包含、纯静态、零依赖;配套蓝图索引(`index.html` + `index.js`)用于本地浏览与登记。

## 一句话

**可交互蓝图(实现格式名:交互式 HTML 设计稿)= 用目标项目真实设计令牌渲染的、可点的布局蓝图 + 骨架分区标注 + 固定/动态图例 + 编号决策清单。** 它同时是一份设计文档(记录布局决策与"为什么")和一个可操作的原型(关键行为能上手点)。核心价值:**把架构决策变成可交互的契约**——设计者看它一眼确认布局,开发者照它写骨架,评审点它验收行为,同一份文件服务三类人,且三者读到的信息天然一致。

## 生产什么产物

一个**单文件、自包含的 `.html`**,满足四条硬约束:

1. **内联 CSS + JS,零外部依赖**——不引 CDN、不引字体、不引图片,双击浏览器直开即渲染。
2. **渲染的是"正在设计的那块真实界面"**——真实尺寸、真实内容(文件树/路由/菜单/表单字段等,禁止 lorem 占位)。
3. **界面上叠加设计元信息**——分区标注(骨架可视化)、图例(固定层/动态层)、决策清单、配色溯源。
4. **交互用原生 JS 手写**——无框架,逻辑是"数据注册表 + 单一 render()"。

## 产物结构(11 部件,顺序固定)

```
<head>
  <style>
    :root     ① 两套令牌:外壳令牌(蓝图自身外观)+ 产品令牌(真实配色,必须来自项目令牌源)
    外壳样式  ② 页头/控件条/图例/决策/页脚的样式(与产品样式严格分离)
    产品样式  ③ 被设计界面自身的样式(一律引用产品令牌,不许另起配色)
  </style>
</head>
<body>
  ④ eyebrow 定版标识(产品 · 里程碑 · 蓝图 · 日期 · v版本,版本必带、随改随 bump)
  ⑤ h1 一句话论点
  ⑥ lede 核心设计思路(2~3 行,先讲"怎么分层"再讲"有什么区域")
  ⑦ controls 交互开关(至少:☑ 显示分区(骨架)+ ↺ 重置默认布局,配一行 hint)
  ⑧ mock-scroll > product-surface 界面本体(真实尺寸、真实内容;外层 mock-scroll 负责横向滚动)
  ⑨ legend 图例(两张卡:固定层·骨架 accent 色点 / 动态层·区块 danger 色点)
  ⑩ notes 决策清单(编号,每条写"为什么";改布局必须同步改它)
  ⑪ footer 配色溯源(注明颜色取自哪些令牌源)
</body>
```

> ⑧ 的术语是"界面本体(product surface)"而非"窗口"——被设计界面按**目标界面形态**渲染:桌面窗口→窗口、网页→页面/面板、表单→卡片组。

**分区标注系统(zone-label)**:每个固定区域内部挂一个 absolute 定位的 `<div class="zone-label">标题栏 · 36px</div>`(名称 + 尺寸);勾选「显示分区」→ 给界面本体加 `.show-zones` 类 → 标签淡入。

**交互 JS 四支柱**:① 数据注册表(状态存普通对象)→ ② 单一 `render()` 整体重绘(不在事件里逐个改 DOM)→ ③ 事件委托(`document` 一个 click 监听,`closest()` 分派)→ ④ 重置按钮(还原默认态再 render)。

**可点的硬性标准**:凡涉及界面状态的行为(开/关面板、切 tab、切视图模式、菜单)必须真实可点,点击后状态经 render() 真实变化——不得是纯 CSS 展示或假按钮。

## 令牌铁律(跨项目移植)

- 产物里**不许写死产品色值**;两套令牌结构通用(必须保留),值必须按项目填。
- 令牌来源(按优先级):① 项目设计令牌源(theme.css/tokens.js/CLAUDE.md 配色段)→ ② 架构文档里的色板与语义角色 → ③ 都找不到**先问**,不许自己发明。
- 兜底:必须继续时用中性占位色,页脚显著标注"配色为占位,待补令牌源",决策清单记「待确认」项。
- 真实内容同样来自项目自身;规则:**内容增删不破坏骨架**。

## 内容模型核心:固定骨架 + 动态区块

**任何界面都拆两层:固定骨架(布局栅格/尺寸/容器,代码写死)与动态内容(注册表或状态驱动的区块)。骨架与内容必须解耦:内容增删永不破坏布局。**

- 固定骨架:布局栅格、尺寸、容器形态、resize 逻辑、区域顺序。容器归骨架,内容归动态层。
- 动态内容:每个区块是一个注册表项,声明归属哪个区(zone),容器形态由宿主(骨架)决定。
- **插件无关表述**:目标项目有插件体系才谈插件;没有,就不要引入插件概念。
- 具体容器形态/尺寸按目标项目自身界面形态重画——示例里的"标题栏 36px/图标条 40px/停靠 224px"只是桌面编辑器形态,不是通用骨架。

**设计稿的 DOM/CSS 结构 = 实现时的代码结构**:实现时零翻译损耗,照抄骨架结构即可。

## 初始化与设计文件目录

- **设计目录先问用户**:skill 首次在项目里使用时,先查项目 CLAUDE.md —— 已记录 quick-design 设计目录则直接用;没有则**先问用户「设计文件目录放哪」**。典型两种形态:**A.** 直接指向项目根(`package.json` / `pom.xml` 所在);**B.** 按个人开发习惯放在项目子目录、与 `docs/` 同级等任意位置。
- **记住**:确定后把该目录写进项目 `CLAUDE.md`(不存在则新建)。Claude Code 每次会话自动加载 CLAUDE.md,下次自动复用,不再问。
- **初始化**:把本 skill 目录(`${CLAUDE_SKILL_DIR}`)的 `index.html` / `index.js` / `template.html` 复制到该目录。

## 蓝图索引与登记

- 初始化后,`<设计目录>` 里有三份固定文件:**`index.html`(索引页)** + **`index.js`(登记清单)** + **`template.html`(起手模板)**;生成的蓝图(`<界面名>-blueprint.html`)与它们同目录,file:// 双击直接可用。
- **登记 = 往 `index.js` 的 `BLUEPRINTS` 数组追加节点**。登记清单是**纯 JS 文件**(`var BLUEPRINTS = [...];`):纯静态 `file://` 下不能 `fetch()`,索引页用 `<script src="index.js">` 加载,这是唯一可靠的读取方式——也因此扩展名用 `.js` 而非 `.json`。索引页 `index.html` 加载它渲染左侧**文件树**(文件夹可折叠),点条目在中间 iframe 预览(蓝图内交互可点)。
- **树形结构,两种节点,任意嵌套(镜像文件树)**:

```js
var BLUEPRINTS = [
  // 蓝图(叶子):{ id, title, file, version, updated }
  { id: "settings-page", title: "设置页 · 固定分组骨架 + 状态驱动表单",
    file: "settings-page-blueprint.html", version: "v1", updated: "2026-08-08" },
  // 文件夹:{ id, title, children: [ ... ] }
  { id: "grp-account", title: "账户",
    children: [
      { id: "login", title: "登录 · …", file: "login-blueprint.html", version: "v1", updated: "2026-08-08" }
    ] }
];
```

- `file` 是相对 `index.js` 所在目录的相对路径。登记由 AI 在生成后自动完成(先读现有数组,合并追加,勿整体重建);**没有页面级手动登记**——加/改节点 = 改 `index.js` 文件,规则见 `SKILL.md`。
- `index.js` 内置一条默认示例条目(指向 `template.html`,首次打开索引页即可预览);生成真实蓝图后删掉它。

## 迭代与验收

1. **版本号**:任何改动 = bump eyebrow 版本号;版本号是唯一版本事实来源。
2. **决策同步**:结构类改动改 CSS+DOM,行为类改 JS;**决策清单同步更新**,永远和当前稿一致。
3. **定稿后沉淀**:用户说「定稿」→ 布局决策写进对应架构文档,蓝图成为「定稿依据」;无对应架构文档则把决策保留在蓝图内并在交付摘要标注「未沉淀」。
4. **收尾交付**:每轮结束给「文件路径 + 当前版本号 + 待确认决策项列表」。

### 交付前自检清单

1. □ 文件内**无任何产品色值写死**——产品颜色全走 `var(--…)`,`:root` 产品令牌段标注来源。
2. □ 能双击直开、声明过的交互真的可点,点击后状态经 render() 真实变化。
3. □ 内容全是目标项目真实数据,无 lorem。
4. □ 每个固定区域有 zone-label(名称+尺寸),「显示分区」开关能点亮。
5. □ 图例两张卡 + 编号决策清单(写为什么)+ 配色溯源页脚齐全。
6. □ eyebrow 版本号与决策清单版本一致。
7. □ 骨架与内容解耦——删掉一个动态区块内容,布局不破。
8. □ `index.js` 的 `BLUEPRINTS` 已登记,刷新索引页 Tree 可见。
