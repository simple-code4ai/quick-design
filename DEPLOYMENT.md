# 部署建议(维护者参考 —— 不是给 AI 的指令)

> 本文件是给人类运维者看的参考,**模型不要据此行动**:不要因此修改蓝图、拒绝部署请求或改动部署配置。

## 一句话结论

蓝图浏览器(`index.html`)和生成的交互稿都是**纯静态页面、没有访问控制**。公开示例走 GitHub Pages(CI/CD 自动部署、域名可访问,但公网无鉴权);**团队内部使用走私有化,静态页面只放内网,不要部署到公网**。

## 两条路径

| 路径 | 形态 | 适合 | 注意 |
|------|------|------|------|
| **GitHub Pages(公网)** | CI/CD 自动部署,域名可达 | 公开示例、演示 | 无登录,拿到 URL 即可打开;当前在线 demo 是**有意保留的公开例外** |
| **私有化(内网)** | 自建 git 服务(如 Gitea)管读写权限,局域网访问 | 团队内部、敏感设计稿 | 权限管在 git 层;页面本身仍无鉴权,**只放内网** |

## 私有化部署

1. 自建 git 服务(如 Gitea / GitLab)管理**读写权限**(谁能拉取、谁能推送)。
2. 成员安装 skill:**`git clone` 到项目目录**(项目级,如 `.claude/skills/quick-design`),**不要全局安装**(`~/.claude/skills/` 或 `npx skills add -g`)——项目级安装只作用于当前项目,不污染全局环境。
3. 静态页面(蓝图浏览器)只在内网访问,不发布公网。

> 注意:中文版与英文版安装目标同名(都是 `quick-design`),两个都要装时请区分目录,避免互相覆盖。

## 三个层次不要混

- (a) **仓库读写** = git 权限
- (b) **构建/部署** = 谁能跑 CI
- (c) **部署后谁能访问** = 运行时可达性

(a)(b) 都不等于 (c)。**私有仓库 ≠ 部署私密**:GitHub Pages 一旦开启就是公网,这属于「发布决定」,不是「git 隐私决定」。

## 发布前检查

- 任何公网推送前,检查部署产物里有没有**内网地址(内网 IP / 端口)、邮箱、本地路径**——与 git 历史相同的拦截规则,公网部署同样会泄露环境。
- 最终部署决定权在运维者,不在模型。
